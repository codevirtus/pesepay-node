/**
 * Compatibility layer presenting the v1.x API surface.
 *
 * ```js
 * const { Pesepay } = require('pesepay/v1-compat');
 * ```
 *
 * @packageDocumentation
 */

/** Marker for the compatibility entry point. */
export const V1_COMPAT: true = true;
